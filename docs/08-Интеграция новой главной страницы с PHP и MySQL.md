# День 8 — Интеграция новой главной страницы с PHP и MySQL
**Дата:** 2026-07-24  
**Статус:** завершено## Фото процесса разработки

> Здесь можно разместить 4–6 снимков:
>
> - создание новой таблицы MySQL;
> - работа API;
> - подключение news-carousel.php;
> - первая новость из базы данных;
> - работа новой карусели;
> - итоговая версия раздела.
>
> Пример:
>
> ```text
> /images/day08/mysql-table.png
> /images/day08/api-json.png
> /images/day08/news-carousel.php.png
> /images/day08/first-news.png
> /images/day08/final-carousel.png
> ```

---

# Цель дня

Перевести раздел **«Наши новости»** с полностью статической HTML-версии на работу через PHP и MySQL, сохранив существующий пользовательский интерфейс без изменения JavaScript-карусели.

Планируемая архитектура:

```text
phpMyAdmin
        ↓
home_feed_items
        ↓
PHP
        ↓
HTML
        ↓
JavaScript-карусель
        ↓
Главная страница
```

---

# Выполненные задачи

## 1. Проектирование новой структуры хранения данных

Создана отдельная таблица публикаций главной страницы:

```text
home_feed_items
```

Структура таблицы включает:

- идентификатор публикации;
- тип контента;
- язык;
- заголовок;
- описание;
- изображение;
- ссылка;
- дата публикации;
- дата события;
- признаки публикации;
- сортировку.

Архитектура хранения:

```text
MySQL
        ↓
home_feed_items
        ↓
Публикации главной страницы
```

---

## 2. Создание первых публикаций

Добавлены тестовые новости непосредственно через phpMyAdmin.

Проверены:

- создание записи;
- сохранение данных;
- изменение публикаций;
- удаление записей;
- сортировка.

Подтверждена корректная работа таблицы.

---

## 3. Разработка серверного API

Создан файл:

```text
home-feed.php
```

Реализованы:

- подключение к MySQL;
- фильтрация по типу;
- фильтрация по языку;
- выбор опубликованных материалов;
- сортировка;
- ограничение количества записей.

Архитектура:

```text
MySQL
        ↓
PHP
        ↓
JSON
```

---

## 4. Проверка JSON

Проверена работа API.

Получены реальные данные:

```text
MySQL
        ↓
PHP
        ↓
JSON
        ↓
Браузер
```

Подтверждено:

- подключение к базе;
- выполнение SQL-запроса;
- корректная сериализация JSON.

---

## 5. Создание серверного генератора карточек

Разработан файл:

```text
news-carousel.php
```

Он выполняет:

```text
MySQL
        ↓
SQL
        ↓
PHP
        ↓
HTML-карточки
```

Каждая карточка формируется автоматически.

---

## 6. Подключение генератора к новой главной странице

Статическая HTML-разметка была заменена серверным подключением:

```php
<?php include __DIR__ . '/../../api/news-carousel.php'; ?>
```

В результате:

```text
index.php
        ↓
include
        ↓
news-carousel.php
        ↓
HTML
```

---

## 7. Проверка интеграции

После подключения выполнены проверки:

- работа include;
- формирование карточек;
- корректность HTML;
- работа существующей карусели.

Подтверждено:

```text
PHP
        ↓
HTML
        ↓
JavaScript
```

без необходимости переписывать JavaScript.

---

## 8. Проверка изображений

Исправлены пути к изображениям.

Проверено отображение:

- изображений;
- дат;
- заголовков;
- описаний;
- ссылок.

Все карточки начали отображаться корректно.

---

## 9. Проверка нескольких публикаций

Через phpMyAdmin добавлены дополнительные записи.

Проверено:

- автоматическое появление карточек;
- сортировка;
- работа существующей карусели;
- корректная загрузка новых материалов.

Архитектура:

```text
phpMyAdmin
        ↓
INSERT
        ↓
MySQL
        ↓
PHP
        ↓
Главная страница
```

---

## 10. Проверка всей цепочки публикации

Полностью протестирован новый цикл публикации.

Получена рабочая схема:

```text
Создание новости
        ↓
phpMyAdmin
        ↓
home_feed_items
        ↓
news-carousel.php
        ↓
index.php
        ↓
HTML
        ↓
content-carousel.js
        ↓
Готовая карточка
```

Ошибок не обнаружено.

---

# Итоговая архитектура

```text
phpMyAdmin
        ↓
home_feed_items
        ↓
SQL
        ↓
PHP
        ↓
HTML
        ↓
CSS
        ↓
JavaScript
        ↓
Готовая карусель
```

---

# Ключевые технические результаты

### Реализована серверная генерация карточек

Теперь публикации создаются не вручную в HTML, а автоматически из базы данных.

---

### Полностью сохранён пользовательский интерфейс

Без изменений продолжили работать:

- CSS;
- JavaScript;
- адаптивность;
- стрелки;
- индикаторы;
- карусель.

---

### Создан фундамент новой CMS

Получена первая рабочая архитектура:

```text
MySQL
        ↓
PHP
        ↓
HTML
        ↓
JavaScript
```

---

### Подтверждена независимость от Tilda

Главная страница больше не зависит от статической HTML-разметки карточек.

Все публикации формируются автоматически.

---

# Изученные технологии

- PHP
- MySQL
- SQL
- phpMyAdmin
- mysqli
- HTML5
- JSON
- include
- GitHub Markdown

---

# Использованные инструменты

- Visual Studio Code
- phpMyAdmin
- Google Chrome
- Chrome DevTools
- ChatGPT

---

# Основной результат дня

Впервые реализована полностью рабочая публикация новостей новой главной страницы непосредственно из MySQL.

Подтверждено:

- подключение базы данных;
- выполнение SQL-запросов;
- автоматическое формирование HTML;
- корректная работа существующей карусели;
- успешная интеграция PHP с новым интерфейсом.

---

# Практическое значение

Получен первый полностью рабочий серверный компонент новой CMS.

Это позволяет:

- отказаться от ручного редактирования HTML;
- создавать публикации непосредственно через базу данных;
- постепенно заменить существующую систему публикации;
- использовать единый механизм формирования главной страницы.

---

# Следующие шаги

1. Добавить поддержку хэштегов публикаций.
2. Реализовать административную форму создания новостей.
3. Добавить редактирование существующих публикаций.
4. Реализовать удаление публикаций.
5. Подключить разделы «События» и «Международные проекты».
6. Создать полноценную административную панель новой CMS.

---

# Итог

Восьмой день стал первым этапом полноценной интеграции новой главной страницы с серверной частью сайта.

```text
phpMyAdmin
        ↓
MySQL
        ↓
PHP
        ↓
HTML
        ↓
JavaScript
        ↓
Работающая главная страница
```

Статическая HTML-разметка раздела **«Наши новости»** успешно заменена динамической генерацией карточек из базы данных при полном сохранении существующего пользовательского интерфейса.

**Статус:** завершено
